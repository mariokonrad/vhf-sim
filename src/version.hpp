#ifndef VHFSIM_VERISION_HPP
#define VHFSIM_VERISION_HPP

namespace vhfsim
{
namespace version
{
const char * project_name();
const char * project_version();
const char * project_version_major();
const char * project_version_minor();
const char * project_version_patch();
const char * project_license();
const char * git_branch();
const char * git_commit_hash();
const char * git_authors();
}
}

#endif
